#include "main.h"
#include <stdio.h>
#include <stdint.h>


/**
 * main - prints its name
 * @argc: num of agr passed to the program
 * @argv: an array of a string
 * Return: 0
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}

